Taches:
Revoir serializer (enlever le friend de instance)
Comparer fonctions et commentaire avec rttr
Essayer la lib dans un projet extern: #pragma warning (registry.inl) et ddl interface
Réparer le sample en x86: recompile gl3w
Gestion des enums
Gestion des conteneur associatif
Optimisation de la serialization
Ajouter des include pour l'utilisateur et un include qui inclue tout ? (register.h -> forward.h par exemple)

Temporaire:
commentaire des views

merge fonction comme AssociativeClear et SequenceClear
aulink vld to binaries ?
rename typedef, pointer sequence
variant ou autre get value mettre reinterpret cast
ajouter sequence et associatif manquant si besoin et test
enlever friend des iterator ?
maybe utility dans typedef ? si compile on s'en fout
essayer d'enlever bout de code useless dans les lambda de associative, partout ?
fusion is_map et autre car useless ?
genraliser l'implementation de conteneur autre que la stl -> has_function with special cas of stl
rajouter des log (dans type ?)
test all sequence avec all type
revoir les cast
clean associative
finir le insert du set
cas du non dynamic dans le load
load save base type ?
create variant d'un vecteur ?
for (auto& si possible
opti de la save avec le moins de copie possible pour éviter de new
argument/instance move
copy move éviter de s'entre appeler ? check sur le net

serializer (for auto&) test, verif les auto